<Window x:Class="WPF_HCI.NewEmailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Compose New Email"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <!-- Define rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Define columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Sender -->
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">From:</TextBlock>
        <TextBox x:Name="SenderBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

        <!-- Recipients -->
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">To:</TextBlock>
        <TextBox x:Name="RecipientsBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"
                 ToolTip="Separate multiple recipients with commas"/>

        <!-- Subject -->
        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">Subject:</TextBlock>
        <TextBox x:Name="SubjectBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"/>

        <!-- Content -->
        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Top">Body:</TextBlock>
        <TextBox x:Name="ContentBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5"
                 AcceptsReturn="True" Height="120" TextWrapping="Wrap"/>

        <!-- Attachment label + button -->
        <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center">Attachments:</TextBlock>
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Text="Add files:" Margin="0,0,5,0"/>
            <Button Content="📎 Add" Click="AddAttachment_Click" Padding="5,0" />
        </StackPanel>

        <!-- Attachment list -->
        <ItemsControl Grid.Row="5" Grid.Column="1" x:Name="AttachmentList" Margin="0,5,0,5">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="📄" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding}" Width="200"/>
                        <Button Content="Remove" Margin="10,0,0,0" Click="RemoveAttachment_Click" Tag="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveDraftBtn" Content="Save as Draft" Margin="0,0,5,0" Width="100" Click="SaveDraft_Click"/>
            <Button x:Name="SendBtn" Content="Send" Width="75" Click="Send_Click"/>
        </StackPanel>
    </Grid>
</Window>
